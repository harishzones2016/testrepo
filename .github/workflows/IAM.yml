name: "01_IAMRoles"
on:
  workflow_dispatch:

jobs:
  Plan_Uat:
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/aws_tf_plan.yml
    with:
      path: ./IAMRoles
      region: ap-south-1
#      tf_vars_file: uat.tfvars
#      gh_environment: uat
#    secrets:
